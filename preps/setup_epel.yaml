---
- hosts: packt-admin
  remote_user: root
  become: no
  gather_facts: false
  become_method: sudo
  tasks:
  - name: Ensure EPEL is enabled 
    yum: 
      name: epel-release 
      state: present 
